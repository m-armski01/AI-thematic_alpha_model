# Runtime dependencies (mirrored in pyproject.toml). Install with: pip install -e ".[dev]"

# --- Core data ---
pandas==2.2.2
numpy==1.26.4

# --- Market & macro data ---
# NOTE: yfinance depends on Yahoo's undocumented endpoints and breaks frequently on old pins.
# 0.2.40 was blocked outright by Yahoo (crumb/auth changes); 1.7.0 confirmed working 2026-09-10.
yfinance==1.7.0
# FRED is read via its keyless CSV endpoint (fredgraph.csv) with pandas directly. pandas-datareader
# 0.10.0 imports `distutils`, which Python 3.12 removed, so it is deliberately NOT used.
# Real exchange session calendars (XNYS, XKRX). Must stay >= 4.13 (2025-01-09 NYSE closure,
# Korean ad-hoc holidays).
exchange_calendars==4.13.2

# --- Storage / config / plotting ---
pyarrow==16.1.0
PyYAML==6.0.1
pydantic==2.7.4
matplotlib==3.9.0
